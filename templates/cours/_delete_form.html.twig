<form method="post" action="{{ path('app_cours_delete', {'id_cours': cour.id_cours}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cour.id_cours) }}">
    <button class="btn">Delete</button>
</form>
