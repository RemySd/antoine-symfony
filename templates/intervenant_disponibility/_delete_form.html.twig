<form method="post" action="{{ path('app_intervenant_disponibility_delete', {'id': intervenant_disponibility.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ intervenant_disponibility.id) }}">
    <button class="btn">Delete</button>
</form>
