<form method="post" action="{{ path('app_intervenant_delete', {'id_intervenant': intervenant.idIntervenant}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ intervenant.idIntervenant) }}">
    <button class="btn">Delete</button>
</form>
