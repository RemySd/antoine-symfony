{% extends 'base.html.twig' %}

{% block title %}Register
{% endblock %}

{% block body %}

	<a href="{{ path('calendrier',{'year': 'now' | date('Y')}) }}">Get full calendar from current year</a>

	<div>
		<a href="{{ path('app_intervenant_index') }}">Intervenant</a>
	</div>
	<div>
		<a href="{{ path('app_matiere_index') }}">Matière</a>
	</div>
	<div>
		<a href="{{ path('app_calendrier_index') }}">calendrier</a>
	</div>
	<div>
		<a href="{{ path('app_cours_index') }}">Cours</a>
	</div>
	<div>
		<a href="{{ path('all_disponibility') }}">Intervenant Disponibilities</a>
	</div>
{% endblock %}
